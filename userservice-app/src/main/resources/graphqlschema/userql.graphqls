type User {
    id: ID!,
    firstName: String,
    lastName: String,
    email: String
}

type Query {
userById(id: ID!): User
}


type Mutation {
createUser(firstName: String!, lastName: String!, email: String!): User
}